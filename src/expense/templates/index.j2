<!doctype html>
<html lang="ja">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Expense</title>
  <link rel="stylesheet" href="/static/style.css?v={{ range(100000, 999999) | random }}" />
  <link rel="manifest" href="/static/manifest.json" />
  <meta name="theme-color" content="#111827">
  <script src="/static/preload.js?v={{ range(100000, 999999) | random }}"></script>
</head>

<body>
  <button id="install-btn" class="btn install" style="display: none;"> {{ icons.get("pwa_install", "📲") }} インストール</button>
  <button id="hamburger-menu-btn" class="small-btn"> ☰ </button>
  <div id="menu-container">
    <form method="get" action="/"><button type="submit" id="home-btn" class="small-btn"> {{ icons.get("home", "🏠") }} 家計簿　 </button></form>
    <form method="get" action="/asset_management"><button type="submit" id="asset-management-btn" class="small-btn"> {{ icons.get("asset_management", "📈") }} 資産管理 </button></form>
    <button id="theme-toggle" class="small-btn"> {% if theme == "dark" %} {{ icons.get("light_theme", "☀️") }} {% else %} {{ icons.get("dark_theme", "🌙") }} {% endif %} テーマ　 </button>
  </div>
  <div class="container">
    <h1> {{ icons.get("title", "") }} Expense </h1>

    <div
      class="msg"
    {% if status and msg %}
      id="success-msg"
    {% elif not status and msg %}
      id="failed-msg"
    {% else %}
      style="display: none;"
    {% endif %}
    >
      {{ msg }}
    {% if info %}
      <p><strong>{{ info | info_to_html }}</strong></p>
    {% endif %}
      <button id="msg-close-btn" type="button" class="btn-close"> ✕ </button>
    </div>

    <div class="card">
      <form method="post" action="/register">
        <h3 class="collapsible-trigger" data-key="register">
          <span> {{ icons.get("register", "🚀") }} 家計簿に登録する情報を入力 </span>
          <span class="collapsible-toggle"></span>
        </h3>
        <div id="register-container" class="collapsible-content">
          <input
            type="date"
            id="expense-date"
            name="expense_date"
            value="{{ expense_date or today }}"
            min="{{ expense_date_range[0] }}"
            max="{{ expense_date_range[1] }}"
            required
          />
          <select id="expense-type" name="expense_type">
            {% for item in items %}
              {% if item.startswith("[HEAD] ") %}
                <option disabled>── {{ item.replace("[HEAD] ", "") }} ──</option>
              {% else %}
                <option value="{{ item }}">
                  {{ item }}
                </option>
              {% endif %}
            {% endfor %}
          </select>
          <input type="number" id="expense-amount" name="expense_amount" value=""
            placeholder="{{ icons.get('input_amount', '💴') }} 金額を入力"
            {% if items | length > 1 and "/" in items[1] %} style="display: none;"
            {% else %} required
            {% endif %} />
          <input type="text" id="expense-memo" name="expense_memo" value="" placeholder="{{ icons.get('input_memo', '✏️') }} メモを入力"
            {% if items | length > 1 and "/" in items[1] %} style="display: none;" {% endif %} />
          <button type="submit" class="btn">＋ 登録</button>
        </div>
      </form>
    </div>

    {% if screenshot_name %}
    <div class="card">
      <form method="post" action="/ocr">
        <h3 class="collapsible-trigger" data-key="ocr">
          <span> {{ icons.get("ocr", "📷") }} QR決済の画像から支払情報を取得 </span>
          <span class="collapsible-toggle"></span>
        </h3>
        <div id="ocr-container" class="collapsible-content">
          <div id="screenshot-name">
            {{ icons.get("screenshot_name", "") }} 対象画像 : {{ screenshot_name.split("/") | last }}
          </div>
          <div id="img-overlay" class="overlay"></div>
          <img id="screenshot" class="screenshot" alt="{{ screenshot_name.split(" /") | last }}"
            src="data:image/png;base64,{{ screenshot_base64 }}"
            onclick="this.classList.toggle('is-zoomed')"
          >
          <button type="submit" class="btn secondary" {% if disable_ocr %} disabled {% endif %}> {% if disable_ocr %} 登録済
            {% else %} 読取実行 {% endif %}</button>
        </div>
      </form>
    </div>
    {% endif %}

    <div class="card">
      <h3 class="collapsible-trigger" data-key="record">
        <span> {{ icons.get("record_table", "🧾") }} 直近の支出履歴 ({{n_records}}件) </span>
        <span class="collapsible-toggle"></span>
      </h3>
      <div id="confirmation-overlay" class="overlay" style="display: none;">
        <div id="confirmation-dialog" class="confirm">
          <h3> {{ icons.get("confirm_dialog", "🛠️") }}  レコードを編集 </h3>
          <button id="confirmation-close-btn" type="button" class="btn-close"> ✕ </button>
          <div class="confirmation-btn-container">
            <form method="post" action="/edit">
              <input type="hidden" id="target-record-date" name="target_date">
              <input type="hidden" id="target-record-type" name="target_type">
              <input type="hidden" id="target-record-amount" name="target_amount">
              <input type="hidden" id="target-record-memo" name="target_memo">
              <input
                type="date"
                id="new-expense-date"
                name="new_expense_date"
                min="{{ expense_date_range[0] }}"
                max="{{ expense_date_range[1] }}"
                required
              />
              <select id="new-expense-type" name="new_expense_type">
                {% for item in items %}
                {% if "/" not in item and not item.startswith("[HEAD] ") %}
                <option value="{{ item }}">
                  {{ item }}
                </option>
                {% endif %}
                {% endfor %}
              </select>
              <input type="text" id="new-expense-amount" name="new_expense_amount" placeholder="{{ icons.get('input_amount', '💴') }} 金額を入力" required>
              <input type="text" id="new-expense-memo" name="new_expense_memo" placeholder="{{ icons.get('input_memo', '✏️') }} メモを入力">
              <div class="space"></div>
              <button type="submit" class="btn-edit"> {{ icons.get("edit_record", "🔧") }} 修正 </button>
            </form>
            <form method="post" action="/delete">
              <input type="hidden" name="expense_date" id="delete-record-date">
              <input type="hidden" name="expense_type" id="delete-record-type">
              <input type="hidden" name="expense_amount" id="delete-record-amount">
              <input type="hidden" name="expense_memo" id="delete-record-memo">
              <button type="submit" class="btn-delete"> {{ icons.get("delete_record", "🗑️") }} 削除 </button>
            </form>
          </div>
        </div>
      </div>
      <a href="{{ gspread_url }}" class="link-spread"> {{ icons.get("link", "🔗") }} スプレッドシートを開く </a>
      <div id="record-container" class="collapsible-content">
        <div class="search-wrapper">
          <input type="text" id="search-input" placeholder="{{ icons.get('search', '🔍') }} 履歴を検索" />
          <button id="clear-search" type="button">✕</button>
        </div>
        <div class="table-container">
          <table>
            <colgroup>
              <col>
              <col>
              <col>
              <col>
            </colgroup>
            <thead>
              <tr>
                <th>日付</th>
                <th>カテゴリ</th>
                <th>金額</th>
                <th>メモ</th>
              </tr>
            </thead>
            <tbody>
              {% for record in records %}
              <tr>
                <td>{{ record.date }}</td>
                <td>{{ record.expense_type }}</td>
                <td>¥{{ "{:,}".format(record.expense_amount) }}</td>
                <td class="memo">{{ record.expense_memo }}</td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
        <div id="total-amount" style="display: none"></div>
      </div>
    </div>

    <div class="card">
      <h3 class="collapsible-trigger" data-key="report">
        <span> {{ icons.get("report", "📊") }} 支出レポート ({{today}}) </span>
        <span class="collapsible-toggle"></span>
      </h3>
      <div id="report-container" class="collapsible-content">
        <div class="summary-list">
          <li> 本日の支出金額 : ¥{{ "{:,}".format(today_total) }} </li>
          <li> 今月の支出金額 : ¥{{ "{:,}".format(monthly_total) }} </li>
          <li> 前月の支出金額 : ¥{{ "{:,}".format(prev_monthly_total) }} </li>
        </div>
        <div class="graph-container">
          {{ plotlyjs | safe }}
          <hr>
          <div id="pie-chart-controls" class="chart-controls"></div>
          <div id="pie-chart-container" class="loading"><div class="spinner"></div></div>
          <hr>
          <div id="daily-chart-controls" class="chart-controls"></div>
          <div id="daily-chart-container" class="loading"><div class="spinner"></div></div>
          <hr>
          <div id="bar-chart-container" class="loading"><div class="spinner"></div></div>
          <hr>
          <div id="annual-fiscal-report-chart-container" class="loading"><div class="spinner"></div></div>
        </div>
      </div>
    </div>

  </div>
  <div id="loader" class="overlay" style="display: none;">
    <div class="spinner"></div>
  </div>
</body>
<script type="module" src="/static/js/main.js"></script>

</html>
