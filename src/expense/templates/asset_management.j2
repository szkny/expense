<!doctype html>
<html lang="ja">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Expense</title>
  <link rel="stylesheet" href="/static/style.css?v={{ range(100000, 999999) | random }}" />
  <link rel="manifest" href="/static/manifest.json" />
  <meta name="theme-color" content="#111827">
  <script src="/static/preload.js?v={{ range(100000, 999999) | random }}"></script>
</head>

<body>
  <button id="install-btn" class="btn install" style="display: none;"> {{ icons.get("pwa_install", "📲") }} インストール</button>
  <button id="hamburger-menu-btn" class="small-btn"> ☰ </button>
  <div id="menu-container">
    <form method="get" action="/"><button type="submit" id="home-btn" class="small-btn"> {{ icons.get("home", "🏠") }} 家計簿　 </button></form>
    <form method="get" action="/asset_management"><button type="submit" id="asset-management-btn" class="small-btn"> {{ icons.get("asset_management", "📈") }} 資産管理 </button></form>
    <button id="theme-toggle" class="small-btn"> {% if theme == "dark" %} {{ icons.get("light_theme", "☀️") }} {% else %} {{ icons.get("dark_theme", "🌙") }} {% endif %} テーマ　 </button>
  </div>
  <div class="container">
    <h1> {{ icons.get("title", "") }} Asset Management </h1>

    <div class="card">
      <h3 class="collapsible-trigger" data-key="asset-report">
        <span> {{ icons.get("asset_portfolio", "📊") }} ポートフォリオ </span>
        <span class="collapsible-toggle"></span>
      </h3>
      <div id="asset-report-container" class="collapsible-content">
        <div class="asset-summary-list">
          <div class="asset-summary-content">
            <span> 資産総額 </span>
            <span class="asset-summary-details"> {{today.year}}年{{today.month}}月{{today.day}}日 {{"{:02d}".format(today.hour)}}:{{"{:02d}".format(today.minute)}}時点 </span>
          </div>
          <div class="asset-summary-display"> {{asset_summary.total}} </div>
          <div class="asset-summary-details">
            <span> 前日比 </span>
            <span
              {% if "¥0" in asset_summary.change %}
                class=""
              {% elif "+¥" in asset_summary.change %}
                class="asset-color-up asset-up"
              {% else %}
                class="asset-color-down asset-down"
              {% endif %}
            > {{asset_summary.change}} </span>
          </div>
          <div class="asset-summary-details">
            <span> ドローダウン </span>
            <span
              {% if "-" in asset_summary.drawdown %}
                class="asset-color-down"
              {% else %}
                class="asset-color-up asset-all-time-high"
              {% endif %}
            > {{asset_summary.drawdown}} </span>
          </div>
          <br>
          <div class="asset-summary-content">
            <span> 含み益 </span>
            <div class="asset-summary-display-sub">
              <span
                {% if "¥0" in asset_summary.profit %}
                  class=""
                {% elif "+¥" in asset_summary.profit %}
                  class="asset-color-up"
                {% else %}
                  class="asset-color-down"
                {% endif %}
              > {{asset_summary.profit}} </span>
            </div>
          </div>
          <div class="asset-summary-details">
            <span> 内 ETF 含み益 </span>
            <span
              {% if "¥0" in asset_summary.profit_etf %}
                class=""
              {% elif "+¥" in asset_summary.profit_etf %}
                class="asset-color-up"
              {% else %}
                class="asset-color-down"
              {% endif %}
            > {{asset_summary.profit_etf}} </span>
          </div>
          <div class="asset-summary-details">
            <span> 損益率 </span>
            <span
              {% if "+0.00%" == asset_summary.roi %}
                class=""
              {% elif "+" in asset_summary.roi %}
                class="asset-color-up"
              {% else %}
                class="asset-color-down"
              {% endif %}
            > {{asset_summary.roi}} </span>
          </div>
          <br>
          <div class="asset-summary-content">
            <span> 為替レート : USD/JPY </span>
            <span class="asset-summary-display-sub"> {{asset_summary.usdjpy}} </span>
          </div>
          <div class="asset-summary-details">
            <span> 前日比 </span>
            <span
              {% if "+0.00%" == asset_summary.change_usdjpy %}
                class=""
              {% elif "+" in asset_summary.change_usdjpy %}
                class="asset-color-up"
              {% else %}
                class="asset-color-down"
              {% endif %}
            > {{asset_summary.change_usdjpy}} </span>
          </div>
        </div>
        <div class="graph-container">
          {{ plotlyjs | safe }}
          <hr>
          <div id="asset-pie-chart-container" class="loading"><div class="spinner"></div></div>
          <hr>
          <div id="asset-heatmap-chart-container" class="loading"><div class="spinner"></div></div>
          <hr>
          <div class="asset-chart-title">
            銘柄別チャート
          </div>
          <select id="symbol-select" class="symbol-selector">
            {% for s in asset_tickers %}
              <option value="{{ s }}">{{ s }}</option>
            {% endfor %}
          </select>
          <div id="asset-tradingview-chart-container"></div>
          <hr>
          <div id="asset-waterfall-chart-container" class="loading"><div class="spinner"></div></div>
          <hr>
          <div id="asset-monthly-history-chart-container" class="loading"><div class="spinner"></div></div>
        </div>
      </div>
    </div>

    <div class="card">
      <h3 class="collapsible-trigger" data-key="asset-record">
        <span> {{ icons.get("asset_list", "📝") }} 資産一覧 </span>
        <span class="collapsible-toggle"></span>
      </h3>
      <a href="{{ gspread_url }}" class="link-spread"> {{ icons.get("link", "🔗") }} スプレッドシートを開く </a>
      <div id="asset-record-container" class="collapsible-content">
        <div class="search-wrapper">
          <input type="text" id="search-input" placeholder="{{ icons.get('search', '🔍') }} 資産をフィルタリング" />
          <button id="clear-search" type="button">✕</button>
        </div>
        <div class="table-container">
          <table>
            <colgroup>
              <col>
              <col>
              <col>
              <col>
              <col>
              <col>
              <col>
              <col>
              <col>
              <col>
            </colgroup>
            <thead>
              <tr>
                <th>銘柄</th>
                <th>保有数</th>
                <th>取得単価(¥)</th>
                <th>現在値($)</th>
                <th>現在値(¥)</th>
                <th>投資額(¥)</th>
                <th>評価額(¥)</th>
                <th>含み益(¥)</th>
                <th>保有比率(%)</th>
                <th>評価損益率(%)</th>
              </tr>
            </thead>
            <tbody>
              {% for item in asset_items %}
              <tr>
                <td>{{ item.ticker }}</td>
                <td>{{ item.num }}</td>
                <td>¥ {{ "{:,.0f}".format(item.acquisition) }}</td>
                <td>$ {{ "{:,}".format(item.price_dollar) }}</td>
                <td>¥ {{ "{:,.0f}".format(item.price) }}</td>
                <td>¥ {{ "{:,.0f}".format(item.invest_amount) }}</td>
                <td>¥ {{ "{:,.0f}".format(item.valuation) }}</td>
                <td>¥ {{ "{:,.0f}".format(item.profit) }}</td>
                <td>{{ "{:.2f}".format(item.weight) }}%</td>
                <td>{{ "{:.2f}".format(item.roi) }}%</td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>

  </div>
  <div id="loader" class="overlay" style="display: none;">
    <div class="spinner"></div>
  </div>
</body>
<script type="module" src="/static/js/main.js"></script>

</html>
